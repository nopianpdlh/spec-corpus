{
  "name": "spec-corpus-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "test": "node --test packages/*/test/**/*.test.js",
    "pack:corpus": "node scripts/pack-workspace.mjs packages/corpus",
    "pack:cli": "node scripts/pack-workspace.mjs packages/cli",
    "verify:release": "node scripts/verify-release.mjs"
  }
}
